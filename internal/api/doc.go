// Package api implements the deeplo admin API served over a Unix socket.
// All endpoints return JSON. File-system permissions on the socket are the
// only access control mechanism; no authentication layer is added.
package api
